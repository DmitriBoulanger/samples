
# Name of the sub-directory with performance data (PNG-images).
# Such a directory is expected to contain 3 child sub-directories with PNG-images 
# Default: performance/ It indicates the directory 
#
# /contex-omr/src/test/resources/de/ityx/contex/omr/houghtransform/data/signature/performance
#
# If new performance data are available, create a new sub-directory as a sibling of the above default 
# and use the latter as an example for the internal structure. Finally, declare it just below

performanceSubdir=performance/
