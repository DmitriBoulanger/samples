CREATE TABLE STUDENTS (STUDENT_ID BIGINT NOT NULL, BIRTH_DATE DATE NOT NULL, FIRST_NAME VARCHAR(35) NOT NULL, LAST_NAME VARCHAR(35), GROUP_ID BIGINT NOT NULL, PRIMARY KEY (STUDENT_ID))
CREATE TABLE STUDENT_GROUPS (GROUP_ID BIGINT NOT NULL, GROUP_NAME VARCHAR(35), PRIMARY KEY (GROUP_ID))
CREATE TABLE STUDENT_GROUPS_STUDENTS (StudentGroup_GROUP_ID BIGINT NOT NULL, students_STUDENT_ID BIGINT NOT NULL, PRIMARY KEY (StudentGroup_GROUP_ID, students_STUDENT_ID))
ALTER TABLE STUDENTS ADD CONSTRAINT STUDENTS_GROUP_ID FOREIGN KEY (GROUP_ID) REFERENCES STUDENT_GROUPS (GROUP_ID)
ALTER TABLE STUDENT_GROUPS_STUDENTS ADD CONSTRAINT STDNTGStdntGrpGRPD FOREIGN KEY (StudentGroup_GROUP_ID) REFERENCES STUDENT_GROUPS (GROUP_ID)
ALTER TABLE STUDENT_GROUPS_STUDENTS ADD CONSTRAINT STDNTGstdntsSTDNTD FOREIGN KEY (students_STUDENT_ID) REFERENCES STUDENTS (STUDENT_ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
